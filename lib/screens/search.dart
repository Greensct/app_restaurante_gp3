import 'package:flutter/material.dart';
/* Clase para buscar recetas */
class Search extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
       child: Text('Vista Buscar')
      ),
    );
  }
}